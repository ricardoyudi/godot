@../godot-claude.md

# Dodge the Creeps

Primeiro projeto, do tutorial oficial 2D, concluído e **já refatorado** com as
convenções acima. Serve de referência: `player.gd`, `mob.gd`, `hud.gd`,
`main.gd` e o autoload `game_state.gd` estão no estilo que os projetos novos
devem seguir desde o início.

- Renomeado de `projeto-teste` em 2026-09-13.
- `GameState` guarda o recorde e persiste em `user://savegame.cfg`.
